# Настройка Apple Shortcuts для Веса (Продвинутая)

Чтобы данные о весе не дублировались и загружались даже за пропущенные дни, мы настроим команду так, чтобы она:
1. Искала записи веса за последние **7 дней**.
2. Отправляла их **списком** (вместе с датой измерения).
3. Наш скрипт сам разберется: если запись за эту дату уже есть — пропустит, если нет — добавит.

---

## Пошаговая инструкция (iOS 15-18)

### 1. Создание новой быстрой команды

1. Открой приложение **Команды** (Shortcuts) → **+** (новая команда).
2. Назови её (например, `Sync Weight Batch`).

### 2. Поиск данных здоровья

1. Добавь действие: **Найти записи здоровья** (Find Health Samples).
2. Настрой фильтр:
   *   Тип: **Вес** (Body Mass).
   *   Фильтр: **Дата начала** находится в **последние 7 дней**.
   *   Сортировка: **Дата начала** (от старых к новым).
   *   Лимит: **Нет** (убрать галочку если есть).

### 3. Цикл обработки (Формирование списка)

Нам нужно собрать JSON список. Самый простой способ — собрать текст и отправить его.

1. Добавь действие: **Повторить с каждым** (Repeat with Each).
   *(Оно автоматически подхватит "Записи здоровья")*.

   **Внутри цикла:**
   
   a. Добавь действие: **Текст** (Text).
   Вставь туда такой шаблон JSON:
   ```json
   {
     "date": "Repeat Item", 
     "weight": "Repeat Item"
   }
   ```
   **Как настроить переменные (Важный момент!):**
   *   Нажми на клавиатуре "Выбрать переменную" → выбери **Повторяемый объект** (Repeat Item). Он встанет в текст как синий овал.
   *   **Для поля date:** Нажми прямо на этот синий овал "Повторяемый объект". В открывшемся меню найди раздел настроек и выбери **Дата начала** (Start Date). Затем выбери Формат даты: **ISO 8601** (убедись, что Время: Нет).
   *   **Для поля weight:** Снова выбери "Повторяемый объект". Нажми на него. Выбери свойство **Значение** (Value).

   b. Добавь действие: **Добавить в переменную** (Add to Variable).
   *   Переменная: `JsonList` (создай новую).
   *   Значение: Результат "Текст" сверху.

2. **Конец повтора**.

### 4. Объединение в массив

Теперь у нас есть куча кусочков JSON. Надо собрать их в один массив `[...]`.

1. Добавь действие: **Объединить текст** (Combine Text).
   *   Объединить: переменную `JsonList`.
   *   Разделитель: **Свой** → `,` (запятая).

2. Добавь действие: **Текст**.
   Впиши:
   ```text
   [
     Объединенный текст
   ]
   ```

### 5. Отправка (POST)

1. Добавь действие: **Получить содержимое URL** (Get Contents of URL).
   *   URL: Твой URL скрипта (`https://script.google.com/...`).
   *   Метод: **POST**.
   *   Заголовки:
       *   `Content-Type`: `application/json`.
   *   Тело запроса: **Файл** (File).
   *   Файл: Результат последнего действия **Текст** (где мы добавили квадратные скобки).

### 6. Уведомление

1. Добавь действие: **Показать уведомление**.
   *   Текст: "Синхронизация завершена".

---

## Как это работает
Каждый раз, когда ты запускаешь эту команду, она берет ВСЕ измерения веса за неделю, упаковывает их и шлет в таблицу. Таблица проверяет даты и сохраняет только то, чего еще нет.

🎉 **Теперь можно не бояться пропустить день или синхронизацию весов!**
